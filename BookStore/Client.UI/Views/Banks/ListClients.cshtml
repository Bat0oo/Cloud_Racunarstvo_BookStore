@model IEnumerable<Client.UI.Models.ClientViewModel>

@{
    ViewData["Title"] = "ListClients";
}

<h1>Spisak klijenata</h1>

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@
<table class="table">
    <thead>
        <tr>
            <th>
               Ime
            </th>
            <th>
                Prezime
            </th>
            <th>
                Datum rodjenja
            </th>
            <th>
                Banka
            </th>
            <th>
                Novcano stanje
            </th>
            <th>
                Tip clanstva
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.Id)
            </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateOfBirth)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BankName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BankAccount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BankMembership)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>*@
            <td>
                @Html.ActionLink("Prebaci novac", "EnlistMoneyTransfer", "Banks", new { userSendId = item.Id })
            </td>
        </tr>
}
    </tbody>
</table>
